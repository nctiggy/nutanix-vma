---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: migrationplans.vma.nutanix.io
spec:
  group: vma.nutanix.io
  names:
    kind: MigrationPlan
    listKind: MigrationPlanList
    plural: migrationplans
    singular: migrationplan
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - description: Max in-flight VMs
      jsonPath: .spec.maxInFlight
      name: VMs
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MigrationPlan is the Schema for the migrationplans API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MigrationPlanSpec defines the desired state of MigrationPlan.
            properties:
              maxInFlight:
                default: 1
                description: |-
                  MaxInFlight limits how many VMs can be migrated concurrently.
                  Defaults to 1.
                minimum: 1
                type: integer
              networkMapRef:
                description: NetworkMapRef references the NetworkMap to use.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              providerRef:
                description: ProviderRef references the NutanixProvider to use.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              storageMapRef:
                description: StorageMapRef references the StorageMap to use.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              targetNamespace:
                description: TargetNamespace is the Kubernetes namespace where migrated
                  VMs will be created.
                type: string
              targetPowerState:
                default: Running
                description: |-
                  TargetPowerState defines whether migrated VMs should be started or left stopped.
                  Defaults to "Running".
                enum:
                - Running
                - Stopped
                type: string
              type:
                default: cold
                description: |-
                  Type is the migration type: "cold" or "warm".
                  Defaults to "cold".
                enum:
                - cold
                - warm
                type: string
              vms:
                description: VMs is the list of VMs to include in this migration plan.
                items:
                  description: PlanVM identifies a VM to include in a migration plan.
                  properties:
                    hooks:
                      description: Hooks lists Hook CRs to run for this VM.
                      items:
                        description: PlanHookRef references a Hook CR and specifies
                          when to run it.
                        properties:
                          hookRef:
                            description: HookRef references the Hook CR.
                            properties:
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                          step:
                            description: 'Step specifies when to run the hook: "PreHook"
                              or "PostHook".'
                            enum:
                            - PreHook
                            - PostHook
                            type: string
                        required:
                        - hookRef
                        - step
                        type: object
                      type: array
                    id:
                      description: ID is the UUID of the Nutanix VM.
                      type: string
                    name:
                      description: Name is the display name of the Nutanix VM (for
                        readability).
                      type: string
                  required:
                  - id
                  type: object
                minItems: 1
                type: array
              warmConfig:
                description: WarmConfig is configuration for warm migration. Only
                  used when Type is "warm".
                properties:
                  maxPrecopyRounds:
                    default: 10
                    description: |-
                      MaxPrecopyRounds limits the number of precopy rounds before requiring cutover.
                      Defaults to 10.
                    type: integer
                  precopyInterval:
                    default: 30m
                    description: |-
                      PrecopyInterval is how often to run incremental syncs (e.g. "30m").
                      Defaults to "30m".
                    type: string
                type: object
            required:
            - networkMapRef
            - providerRef
            - storageMapRef
            - targetNamespace
            - vms
            type: object
          status:
            description: MigrationPlanStatus defines the observed state of MigrationPlan.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the plan's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: Phase is the current lifecycle phase of the plan.
                enum:
                - Pending
                - Validating
                - Ready
                - Error
                type: string
              vms:
                description: VMs is the per-VM validation status.
                items:
                  description: VMValidationStatus captures the validation result for
                    a single VM in a plan.
                  properties:
                    concerns:
                      description: Concerns lists validation findings for this VM.
                      items:
                        description: Concern is a validation finding for a VM.
                        properties:
                          category:
                            description: Category is the severity of the concern.
                            enum:
                            - Error
                            - Warning
                            - Info
                            type: string
                          message:
                            description: Message describes the concern.
                            type: string
                        required:
                        - category
                        - message
                        type: object
                      type: array
                    id:
                      description: ID is the UUID of the Nutanix VM.
                      type: string
                    name:
                      description: Name is the display name of the VM.
                      type: string
                  required:
                  - id
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
